<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KickRoll.App.Views.SessionsListPage"
             Title="課表列表">

    <VerticalStackLayout Padding="10" Spacing="10">
        <Button Text="重新載入" Clicked="OnReloadClicked" />

        <CollectionView x:Name="SessionsCollection"
                        SelectionMode="Single"
                        SelectionChanged="OnSessionSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BorderColor="Gray" CornerRadius="8" Padding="10" Margin="5">
                        <VerticalStackLayout>
                            <Label Text="{Binding TeamName}" FontAttributes="Bold" FontSize="18" />
                            <Label Text="{Binding Location}" />
                            <Label Text="{Binding StartAt}" />
                            <Label Text="{Binding EndAt}" />
                            <Label Text="{Binding CapacityDisplay}" FontAttributes="Bold" TextColor="Blue" />
                            <Label Text="{Binding Status}" TextColor="Green" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Label x:Name="ResultLabel" TextColor="Red" />
    </VerticalStackLayout>
</ContentPage>
